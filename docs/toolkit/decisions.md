---
tool: `scripts/reels/decisions.sh <raw dir> <out dir>`
section: run
who: video
covers: scripts/reels/decisions.sh
---
The landing page's "Decisions you can see" loop, from a `npm run feature-media -- --only site-cheque,site-whiteboard,site-visitor --keep-raw` render: the novelty cheque, the pivot whiteboard and the first user test, about 2 s each with hard cuts, each cropped round its subject at native pixels (the cheque and board from 4K recordings). It writes the muted 1280x720 MP4 and WebM and a poster from the loop's first frame.
