---
tool: `scripts/lib/run-parallel.sh`
section: ci
who: integrator
covers: scripts/lib/run-parallel.sh
---
Runs commands side by side, each with its own vite dependency cache (`HITL_VITE_CACHE`), and prints their output in order.
