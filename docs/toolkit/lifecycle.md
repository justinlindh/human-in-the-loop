---
tool: `npm run lifecycle`
section: browser
covers: scripts/lifecycle.js
---
The real UI end to end: title, founding, play, save, reload, Continue. Fails on any page error. CI runs `--quality low --no-shots`.
